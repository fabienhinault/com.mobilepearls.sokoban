## Pull Request Title

Migrate project config: compileSdk/targetSdk -> 35

---

## Pull Request Body

Met à jour compileSdk et targetSdk vers API 35 (Android 15). Met à jour Gradle/AGP et le wrapper. Prépare la migration vers AndroidX (android.useAndroidX=true, enableJetifier). Retire uses-sdk du manifest et centralise min/target dans Gradle.

Note: la compilation peut nécessiter des adaptations du code (migration AndroidX, FileProvider, permissions runtime, Scoped Storage, etc.). Tester sur un émulateur Android 15 et corriger les erreurs de compilation/compatibilité.